#include "collision.h"
#include "player.h"
#include "cactus.h"
// tice

bool check_collisions(void) {
	int i;
	for (i = 0; i < MAX_CACTUS_COUNT; i++) {
		if (!cacti[i].active) {
			continue;
		}


	}
}
